<Project>
  <!-- Customize analyzer settings for test projects -->
  <PropertyGroup Condition="$(MSBuildProjectName.EndsWith('.Tests'))">
    <!-- Disable CA1515 for test projects as test classes are intentionally public -->
    <NoWarn>$(NoWarn);CA1515</NoWarn>
  </PropertyGroup>
</Project> 